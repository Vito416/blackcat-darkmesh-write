# Rotate Write WAL safely (copytruncate to keep forwarder running)
# Path aligned with WRITE_WAL_PATH in ops/env.prod.example
/var/lib/ao/write-wal.ndjson {
    daily
    rotate 7
    copytruncate
    compress
    notifempty
    missingok
    dateext
}
