name: Secrets Lint

on:
  push:
    paths:
      - "ops/env.prod.example"
      - "ops/verify/secrets_lint.sh"
      - ".github/workflows/secrets-lint.yml"
  pull_request:
    paths:
      - "ops/env.prod.example"
      - "ops/verify/secrets_lint.sh"
      - ".github/workflows/secrets-lint.yml"
  workflow_dispatch: {}

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run secrets lint
        run: bash ops/verify/secrets_lint.sh
