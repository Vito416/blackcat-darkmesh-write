# Sample Commands (placeholders)

- `save-draft-page.json` — draft page command with `Action=SaveDraftPage`.
- `publish-page-version.json` — publish command with `Expected-Version` guard.
- `assign-role.json` — role assignment with tenant scope.
- Upcoming (placeholder): PSP/webhook and order-lifecycle command samples will be added when the PSP abstraction and order state machine land, so ingest/batch smokes can cover those flows.

Use these as inputs for local simulations and future contract tests.
