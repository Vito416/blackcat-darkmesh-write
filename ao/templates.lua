-- Stub templates module for write process; hyperengine/AOS process.lua expects require("templates").
-- We do not bundle any UI templates in this process, so return empty table.
local M = {}
return M
